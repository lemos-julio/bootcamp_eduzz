const assert = require('assert')
const arr = ['fruta','b', 'c', 2,5,7]
function filter_list(l){
    assert.deepEqual({arr}, false,)
    return l
}

var filtred = arr.filter(filter_list)
console.log(filtred)


//((2*B1_COMP)+(2*B1_LARG)+42)*(2*B1_LARG+B1_ALT+12) 
((2*B1_COMP)+(2*B1_LARG)+47)*(2*B1_LARG+B1_ALT+6)                                                                             
